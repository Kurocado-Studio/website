export interface DribbbleShot {
  title: string;
  thumbnail: string;
}

export interface FrontEndProject {
  imgUrl: string;
  title: string;
  description: string;
  categories?: Array<string>;
  url: string;
}
